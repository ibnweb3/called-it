{
  "name": "called-it",
  "version": "0.1.0",
  "private": true,
  "description": "Called It — a 15-minute prediction game where the house is a vault anyone can own. On DreamDEX Event Contracts (Somnia).",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "croupier": "npm run start -w croupier",
    "croupier:dev": "npm run dev -w croupier",
    "backend": "npm run start -w backend",
    "backend:dev": "npm run dev -w backend",
    "web:dev": "npm run dev -w web",
    "web:build": "npm run build -w web",
    "vault-web:dev": "npm run dev -w vault-web",
    "chain:typecheck": "npm run typecheck -w @called-it/chain",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "contracts:build": "cd contracts && forge build",
    "contracts:test": "cd contracts && forge test",
    "site:build": "npm run build -w web && npm run build -w vault-web && node deploy/assemble.mjs",
    "deploy": "npm run site:build && wrangler pages deploy deploy/dist --project-name calledit-somnia --branch main --commit-dirty=true"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "wrangler": "^4.129.0"
  }
}
